package medium;

import com.google.common.collect.Lists;

import java.util.ArrayList;
import java.util.Comparator;
import java.util.List;

public class KCLosestPointsToOrigin {

    public static void main(String[] args) {

        //[[1,3],[-2,2]]

    }

//    public int[][] kClosest(int[][] points, int K) {
//
//        if (K == points.length) return points;
//
////        List<Pair<int[], Long>> distances = new ArrayList<>();
////        int[][] result = new int[K][];
////
////        for (int i = 0; i < points.length; i++) {
////
////            long dist = (points[i][0] * points[i][0]) + (points[i][1] * points[i][1]);
////            distances.add(new Pair<>(points[i], dist));
////
////        }
////
////        distances.sort(Comparator.comparing(Pair::getValue));
////
////        for (int i = 0; i < K; i++) {
////            result[i] = distances.get(i).getKey();
////        }
////
////        return result;
//
//    }

}
